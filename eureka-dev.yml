server:
  port: 6868 #服务端口
eureka:
  client:
    register-with-eureka: false #是否将自己注册到eureka中，本身不需要注册
    fetch-registry: false #是否从eureka中获取注册信息
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka/